# 1.36+ improves widget identity / fragment behavior (fewer blank-page + Button chunk issues).
streamlit>=1.36.0
# IANA time zone database for Linux/Streamlit Cloud (ZoneInfo needs this without full OS tzdata).
tzdata>=2024.1
pandas>=2.0.0
# Joblib/sklearn bundles in this repo were built with NumPy 2.x; NumPy 1.x on Cloud breaks unpickling (numpy._core).
numpy>=2.0.0,<2.3.0
scipy>=1.11.0
# Keep sklearn in a narrow band so Cloud pip does not drift from the stack used to train joblibs.
scikit-learn>=1.5.0,<1.7.0
joblib>=1.3.0
pyarrow>=14.0.0
requests>=2.31.0
# Optional: Statcast helpers only; core app uses MLB Stats API (FanGraphs often 403s scrapers).
pybaseball>=2.2.7
